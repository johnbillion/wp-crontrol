# yaml-language-server: $schema=https://json.schemastore.org/github-workflow

name: Acceptance Tests
on:
  push:
    branches:
      - 'develop'
      - 'trunk'
    paths:
      - '.github/workflows/acceptance-tests.yml'
      - 'tests/acceptance/**'
      - '**.css'
      - '**.js'
      - 'src/**'
      - 'wp-crontrol.php'
      - 'composer.json'
      - 'docker-compose.yml'
      - 'tsconfig.json'
  pull_request:
    branches:
      - '**'
    paths:
      - '.github/workflows/acceptance-tests.yml'
      - 'tests/acceptance/**'
      - '**.css'
      - '**.js'
      - 'src/**'
      - 'wp-crontrol.php'
      - 'composer.json'
      - 'docker-compose.yml'
      - 'tsconfig.json'
  workflow_dispatch:

permissions: {}

jobs:
  test:
    name: WP ${{ matrix.wp }}
    permissions:
      contents: read
    strategy:
      # See the following for PHP compatibility of WordPress versions:
      # https://make.wordpress.org/core/handbook/references/php-compatibility-and-wordpress-versions/
      matrix:
        wp:
          # Latest three stable:
          - '6.8'
          - '6.7'
          - '6.6'
        php:
          - '8.3'
          - '7.4'
        include:
          # Latest stable on PHP 8.4:
          - wp: '6.8'
            php: '8.4'
          # Oldest supported on PHP 7.4:
          - wp: '6.3'
            php: '7.4'
      fail-fast: false
    uses: johnbillion/plugin-infrastructure/.github/workflows/reusable-acceptance-tests.yml@8d0915f3452432a1bc65ba9634a3bc608474b7b3
    with:
      node: false
      php: ${{ matrix.php }}
      wp: ${{ matrix.wp }}
