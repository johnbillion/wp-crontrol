# yaml-language-server: $schema=https://json.schemastore.org/github-workflow

name: Coding Standards
on:
  push:
    branches:
      - 'develop'
      - 'trunk'
    paths:
      - '.github/workflows/coding-standards.yml'
      - '**.css'
      - '**.js'
      - '**.php'
      - 'composer.json'
      - 'phpcs.xml.dist'
      - 'phpcs56.yml'
      - 'phpstan.neon.dist'
  pull_request:
    branches:
      - '**'
    paths:
      - '.github/workflows/coding-standards.yml'
      - '**.css'
      - '**.js'
      - '**.php'
      - 'composer.json'
      - 'phpcs.xml.dist'
      - 'phpcs56.yml'
      - 'phpstan.neon.dist'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.label }}
    uses: johnbillion/plugin-infrastructure/.github/workflows/reusable-coding-standards.yml@3b1f05490d9c3ad05f058d7c4eeff6ff2173ded0
    strategy:
      matrix:
        label:
          - 'PHP'
        php:
          - '8.3'
          - '7.4'
      fail-fast: false
    with:
      php: ${{ matrix.php }}
